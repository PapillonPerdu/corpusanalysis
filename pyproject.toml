[tool.poetry]
name = "corpusanalysis"
version = "0.1.0"
description = "For primary and secondary sources analysis."
authors = ["Alain Herreman"]

[tool.poetry.dependencies]
python = "^3.8"
numpy = "^1.22.3"
pandas = "^1.4.1"
sympy = "^1.10"
networkx = "^2.7.1"
matplotlib = "^3.5.1"
ipywidgets = "^7.6.5"
ipython = "^8.1.1"
tabulate = "^0.8.9"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

